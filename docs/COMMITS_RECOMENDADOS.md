# Commits recomendados

Comandos sugeridos para documentar cambios:

git add .
git commit -m "docs: agregar documentacion de autoria del sistema"
git push origin main

git add .
git commit -m "docs: documentar historial de evolucion del Catalogo Rodeo B2B"
git push origin main

git add .
git commit -m "docs: agregar preparacion para registro legal del software"
git push origin main

Para futuras mejoras funcionales:

git add .
git commit -m "feat: agregar modo hibrido inteligente para imagenes remotas"
git push origin main
